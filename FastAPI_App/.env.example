DATABASE_URL=postgresql+asyncpg://postgres:postgres@localhost:5432/docprocessor
# TEST_DATABASE_URL is only needed if running alembic migrations locally against the test DB.
# When running via Docker (recommended), the migration command overrides this automatically.
TEST_DATABASE_URL=postgresql+asyncpg://postgres:postgres@localhost:5432/docprocessor_test
TEMPORAL_HOST=localhost:7233
